# Runtime dependencies — PINNED to the locally-tested stack so Streamlit Cloud
# reproduces a known-good build instead of resolving to the newest releases
# (Streamlit 1.61.1 breaks on pandas 3.x). Python is pinned in .python-version.
# Keep this lean: the app reads parquets only. Scraping deps (soccerdata /
# Selenium) live in requirements-etl.txt.
streamlit==1.61.1
pandas==2.3.3
numpy==2.2.6
pyarrow==24.0.0
plotly==6.9.0
mplsoccer==1.8.0
matplotlib==3.10.9
