# Runtime dependencies — what the deployed Streamlit app needs (reads parquets
# only). Keep this lean so Streamlit Community Cloud builds fast and reliably.
# ETL/scraping deps (soccerdata, Selenium, ...) live in requirements-etl.txt.
streamlit>=1.36
pandas>=2.2
pyarrow>=16
plotly>=5.22
mplsoccer>=1.2
matplotlib>=3.8
numpy>=1.26
